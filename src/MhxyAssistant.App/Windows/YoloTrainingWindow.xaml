<Window x:Class="MhxyAssistant.App.Windows.YoloTrainingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="YOLO Training" Height="620" Width="820"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="YOLO training and auto-label" FontSize="18" FontWeight="SemiBold" />

        <Grid Grid.Row="1" Margin="0,18,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="140" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Python" VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding PythonPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,8" />
            <TextBlock Grid.Row="0" Grid.Column="2" Text="Device" VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Device, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Training script" VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding ScriptPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Auto-label script" VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding AutoLabelScriptPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Dataset YAML" VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding DatasetPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,8" />
            <TextBlock Grid.Row="3" Grid.Column="2" Text="Images" VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="3" Grid.Column="3" Text="{Binding AutoLabelImagesPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Base model" VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding ModelPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,8" />
            <TextBlock Grid.Row="4" Grid.Column="2" Text="Run name" VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="4" Grid.Column="3" Text="{Binding RunName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

            <TextBlock Grid.Row="5" Grid.Column="0" Text="Output path" VerticalAlignment="Center" Margin="0,0,12,0" />
            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0" />
            <StackPanel Grid.Row="5" Grid.Column="2" Grid.ColumnSpan="2" Orientation="Horizontal">
                <TextBlock Text="Epochs" VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBox Text="{Binding Epochs, UpdateSourceTrigger=PropertyChanged}" Width="48" Margin="0,0,8,0" />
                <TextBlock Text="Batch" VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBox Text="{Binding Batch, UpdateSourceTrigger=PropertyChanged}" Width="42" Margin="0,0,8,0" />
                <TextBlock Text="Img" VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBox Text="{Binding ImageSize, UpdateSourceTrigger=PropertyChanged}" Width="48" Margin="0,0,8,0" />
                <TextBlock Text="Conf" VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBox Text="{Binding Confidence, UpdateSourceTrigger=PropertyChanged}" Width="48" />
            </StackPanel>
        </Grid>

        <TextBox Grid.Row="2"
                 Text="{Binding LogText, Mode=OneWay}"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12" />

        <DockPanel Grid.Row="3" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" TextWrapping="Wrap" />
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Auto Label" Width="96" Margin="8,0,0,0" Command="{Binding AutoLabelCommand}" />
                <Button Content="Start Training" Width="112" Margin="8,0,0,0" Command="{Binding StartTrainingCommand}" />
                <Button Content="Stop" Width="84" Margin="8,0,0,0" Command="{Binding StopCommand}" />
                <Button Content="Save" Width="84" Margin="8,0,0,0" Command="{Binding SaveCommand}" />
                <Button Content="Close" Width="84" Margin="8,0,0,0" Click="Close_OnClick" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
