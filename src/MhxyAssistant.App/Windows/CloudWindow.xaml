<Window x:Class="MhxyAssistant.App.Windows.CloudWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cloud" Height="500" Width="760"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Cloud storage" FontSize="18" FontWeight="SemiBold" />

        <Grid Grid.Row="1" Margin="0,18,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Server URL" VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding ServerUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Upload file" VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding UploadPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Download dir" VerticalAlignment="Center" Margin="0,0,12,0" />
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding DownloadDirectory, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0" />
            <TextBlock Grid.Row="2" Grid.Column="2" Text="Busy" VerticalAlignment="Center" Margin="0,0,12,0" />
            <CheckBox Grid.Row="2" Grid.Column="3" IsChecked="{Binding IsBusy, Mode=OneWay}" IsEnabled="False" VerticalAlignment="Center" />
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding ServerItems}"
                  SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="120" />
                <DataGridTextColumn Header="Modified" Binding="{Binding ModifiedAt}" Width="160" />
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="3" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" TextWrapping="Wrap" />
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Health" Width="84" Margin="8,0,0,0" Command="{Binding HealthCommand}" />
                <Button Content="Refresh" Width="84" Margin="8,0,0,0" Command="{Binding RefreshCommand}" />
                <Button Content="Upload" Width="84" Margin="8,0,0,0" Command="{Binding UploadCommand}" />
                <Button Content="Download" Width="84" Margin="8,0,0,0" Command="{Binding DownloadCommand}" />
                <Button Content="Delete" Width="84" Margin="8,0,0,0" Command="{Binding DeleteCommand}" />
                <Button Content="Save" Width="84" Margin="8,0,0,0" Command="{Binding SaveCommand}" />
                <Button Content="Close" Width="84" Margin="8,0,0,0" Click="Close_OnClick" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
